package pt.tecnico.mydrive.presentation;
// imports missing

public class List extends Command {

	public List(Shell sh) {
    	super(sh, "list", "lists the files on the current directory, or, if given the path of the directory, lists the files in that directory");
    }

	public void execute(String[] args) {
		
		
	}

	
}
